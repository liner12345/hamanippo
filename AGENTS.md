# 配送日報 — プロジェクト憲章・開発ガイドライン

> このファイルは `配送日報` プロジェクトの設計・開発における単一の真実の源（SSoT）です。
> Antigravity エージェントおよび開発者は本ファイルの指示に従って作業を行ってください。

---

## 1. プロジェクト概要

- **名称**: 配送日報 (hamanippo)
- **目的**: 配送ドライバーが訪問先をタップ操作で素早く記録し、LINEやメッセージアプリ等へそのまま貼り付けられる日報テキストを簡単に作成できるPWAツール。
- **ターゲット環境**: スマートフォン（iOS Safari / Android Chrome）およびPCブラウザ。レスポンシブかつ片手操作を優先したUI。
- **デプロイ形態**: GitHub Pages（静的ファイル配信・ビルドプロセスなし）。

---

## 2. 技術スタック・アーキテクチャ

- **HTML/CSS/JS**: Vanilla HTML5 / Vanilla CSS3 / Standard JavaScript (ES5+)
- **フレームワーク / ビルドツール**: なし（`npm`, `vite`, `webpack` 等のビルドツールは使用せず、`index.html` をブラウザでそのまま読み込んで動作させる）
- **データ保持**: ブラウザの `localStorage`（キー: `nippou.v1`）。オフライン時や閉じた場合もメモリ保持フォールバック機能付き。
- **オフライン対応**: Service Worker (`sw.js`) & Web App Manifest (`manifest.webmanifest`) によるPWA対応。

---

## 3. ファイル構造

```
hamanippo/
├── index.html            # メイン画面構造・各シート/モーダル定義
├── styles.css            # アプリ全体のスタイリング（CSS変数、レスポンシブ対応）
├── app.js                # アプリケーションロジック（状態管理、描画、イベント処理、ストレージ）
├── sw.js                 # PWA Service Worker（キャッシュ・オフライン動作）
├── manifest.webmanifest  # PWA マニフェスト設定
├── README.md             # ユーザー向け説明・導入手順
├── AGENTS.md             # 本ファイル。設計・開発の単一の真実の源（SSoT）
├── GEMINI.md             # AGENTS.md への参照のみ（Gemini系エージェント用の入口）
├── .gitignore            # 除外ファイル設定
└── icon*.png / icon.svg  # アイコン素材
```

---

## 4. 開発・変更時のガイドライン

1. **外部依存パッケージを追加しない**:
   - サードパーティのCSS/JSライブラリは導入せず、Vanillaなコードでシンプルかつ軽量に維持してください。
2. **片手操作と高い視認性を維持する**:
   - ボタンやインタラクティブ要素のタップ領域は十分な広さ（`min-height: 44px〜52px` 程度）を確保し、画面下部の操作性を重視してください。
3. **ビルド不要の構成を保つ**:
   - `index.html` から相対パスで CSS/JS を読み込んでいるため、どの環境でもファイルをブラウザで直接開くだけで動く構造を守ってください。
4. **文字列の切り詰めを識別に使う画面へ持ち込まない**:
   - 配送先ピッカーやコース編集など「どれを選ぶか判断する」画面では、`white-space:nowrap` による一行省略を使わないでください。長い配送先名の末尾（「北棟」「南棟」など）が消え、別々の配送先が同一表示になります。折り返し（`-webkit-line-clamp` による複数行）で対応してください。
   - 一行省略を使ってよいのは、履歴一覧の要約行のように、切れても他の情報（日付・件数）で識別できる箇所に限ります。
5. **ページの独立性を壊さない**:
   - 1日の記録は `reports[日付].pages[]` に分かれます。出力・クリア・転送・並べ替えは、すべて選択中の1ページの中だけで完結させてください。ページをまたいで行を参照したり、複数ページを連結して1つの日報として出力したりしてはいけません。午前と午後を分けて別々に送れることが、この構造の目的です。
   - 新しい操作を足すときは `curPage()` / `curStops()` を経由してください。`report(k)` から直接 `stops` を触るコードは、v3以降のデータ構造には存在しません。
6. **データ構造を変えたら `migrate()` に移行処理を書く**:
   - `app.js` の `migrate()` は起動時とバックアップ読み込み時の両方から呼ばれます。移行処理をこの関数の外に書くと、旧形式のバックアップを読み込んだ経路で移行が走らず、転送の紐付けなどが失われます。
   - 移行後は `save()` で書き戻してください。書き戻さないと毎回の起動で移行が再実行されます。
7. **画面下端に固定するバーは下方向へ延長する**:
   - iOS Safari は、スクロールできないページで `position:fixed; bottom:0` の要素をレイアウトビューポート下端（ツールバー分だけ短い）に固定します。このため短いページでだけバーの下に隙間が出ます。`.tabbar::after` で下へ伸ばして塞いでいるので、この擬似要素を消さないでください。隙間が無い端末では画面外に出るだけで害はありません。
8. **`sw.js` のキャッシュ名を更新する**:
   - `index.html` / `styles.css` / `app.js` のいずれかを変更したら、`sw.js` 冒頭の `CACHE` の版数を必ず上げてください。上げ忘れると利用者の端末に旧版が残ります。
9. **動作確認手順**:
   - 変更後は `index.html` をブラウザで直接開き、日報の追加・編集・クリア・トースト表示・設定変更などの各種画面の表示と動作を確認してください。
   - あわせて次の条件でも確認してください。
     - 幅375pxの狭い画面
     - 20文字以上の長い配送先名（末尾まで識別できるか）
     - ダークモード（ヘッダーと文字のコントラスト）
     - タップ領域が44px以上あるか（特に破壊的操作のボタン）
     - 日報・配送先・履歴の3タブを切り替え、最下部までスクロールしたときの余白が揃っているか
     - 旧版のバックアップ（`v` が現在値より小さいJSON）を読み込んで内容が保たれるか
     - 1日に2ページ作り、片方の操作がもう片方に影響しないか
     - スクロールが発生しない短いページで、タブバーの下に隙間が出ないか（実機のSafariで確認）
